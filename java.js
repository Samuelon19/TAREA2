//////////secciones

        function showSection1(id) {
            var secciones = document.querySelectorAll(".seccion1");
            secciones.forEach(function(seccion) {
                seccion.style.display = "none";
            });

            var seccion = document.getElementById(id);
            seccion.style.display = "block";
        }

        function showSection2(id) {
            var secciones = document.querySelectorAll(".seccion2");
            secciones.forEach(function(seccion) {
                seccion.style.display = "none";
            });

            var seccion = document.getElementById(id);
            seccion.style.display = "block";
        }

        function showSection3(id) {
            var secciones = document.querySelectorAll(".seccion3");
            secciones.forEach(function(seccion) {
                seccion.style.display = "none";
            });

            var seccion = document.getElementById(id);
            seccion.style.display = "block";
        }

        function showSection4(id) {
            var secciones = document.querySelectorAll(".seccion4");
            secciones.forEach(function(seccion) {
                seccion.style.display = "none";
            });

            var seccion = document.getElementById(id);
            seccion.style.display = "block";
        }

        function showSection5(id) {
            var secciones = document.querySelectorAll(".seccion5");
            secciones.forEach(function(seccion) {
                seccion.style.display = "none";
            });

            var seccion = document.getElementById(id);
            seccion.style.display = "block";
        }

        function showSection6(id) {
            var secciones = document.querySelectorAll(".seccion6");
            secciones.forEach(function(seccion) {
                seccion.style.display = "none";
            });

            var seccion = document.getElementById(id);
            seccion.style.display = "block";
        }

        function showSection7(id) {
            var secciones = document.querySelectorAll(".seccion7");
            secciones.forEach(function(seccion) {
                seccion.style.display = "none";
            });

            var seccion = document.getElementById(id);
            seccion.style.display = "block";
        }

        function showSection8(id) {
            var secciones = document.querySelectorAll(".seccion8");
            secciones.forEach(function(seccion) {
                seccion.style.display = "none";
            });

            var seccion = document.getElementById(id);
            seccion.style.display = "block";
        }

        function showSection9(id) {
            var secciones = document.querySelectorAll(".seccion9");
            secciones.forEach(function(seccion) {
                seccion.style.display = "none";
            });

            var seccion = document.getElementById(id);
            seccion.style.display = "block";
        }

        function showSection10(id) {
            var secciones = document.querySelectorAll(".seccion10");
            secciones.forEach(function(seccion) {
                seccion.style.display = "none";
            });

            var seccion = document.getElementById(id);
            seccion.style.display = "block";
        }

        function showSection11(id) {
            var secciones = document.querySelectorAll(".seccion11");
            secciones.forEach(function(seccion) {
                seccion.style.display = "none";
            });

            var seccion = document.getElementById(id);
            seccion.style.display = "block";
        }